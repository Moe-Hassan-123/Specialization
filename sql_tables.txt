CREATE TABLE students (id INTEGER PRIMARY KEY,
                       name TEXT NOT NULL,
                       password BLOB NOT NULL,
                       national_id BLOB NOT NULL,
                       grade INT NOT NULL CHECK (grade IN (11, 12)),
                       section TEXT NOT NULL CHECK (section IN ("scientific","literature",
                                                                "math_oriented","science_oriented"))
                      );

